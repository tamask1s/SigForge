def SineAndSpectrum(sine_name, spec_name, frequencies)
{
    CreateSine(sine_name, 1000, 1000, frequencies);
    FullSpectrum(sine_name, spec_name, 1);
};

SineAndSpectrum(sin_1, spec_1, 5 50 100);
SineAndSpectrum(sin_2, spec_2, 15 150 200);

DisplayData(spec_1, ,2D_map);
DisplayData(spec_2, ,2D_map);